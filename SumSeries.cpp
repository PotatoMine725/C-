#include <stdio.h>
#include <math.h>

int Cal (int *ds,int n) {
    int sum = 0;
    for ( i = 0; i < n; i++)
    {
        sum = sum + (ds[i]*pow(x,i))
    }
    return sum;
}

void nhap(int *ds, int spt) {
    for (int i = 0; i < spt; i++)
    {
        printf("Nhap phan tu thu %d", i+1);
    }
}

int main()
{
    int *ds,spt;
    printf("Nhap so luong phan tu mang: ");
    scanf("%d",&spt);
    ds = new int[spt];
}


